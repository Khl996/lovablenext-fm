# دليل نظام الصلاحيات - Permissions System Guide

## 📋 الوضع الحالي - Current Situation

### ✅ النظام الجديد مطبق ويعمل:

1. **قاعدة البيانات**:
   - جدول `system_roles` - الأدوار (8 أدوار)
   - جدول `permissions` - 22 صلاحية (بدون Work Orders)
   - جدول `role_permissions` - صلاحيات افتراضية لكل دور
   - جدول `user_permissions` - صلاحيات استثنائية

2. **النظام الآن يقرأ من قاعدة البيانات**:
   - ✅ `usePermissions` hook يقرأ من `role_permissions` و `user_permissions`
   - ✅ `useCurrentUser` يوفر `canAccessAdmin` بناءً على الصلاحيات
   - ✅ `AppSidebar` يعرض/يخفي القوائم بناءً على الصلاحيات
   - ✅ جميع الـ modules (عدا Work Orders) تستخدم النظام الجديد

3. **صلاحيات Work Orders**:
   - ✅ **مثبتة ومثالية** في `rolePermissions.ts`
   - ⚠️ **لا تلمس** - النظام القديم يعمل بشكل مثالي

### 🎉 ما تم تنفيذه:
- ✅ Database migration بـ 22 صلاحية + role permissions
- ✅ تعديل usePermissions ليدعم role_code
- ✅ تعديل useCurrentUser لإضافة canAccessAdmin
- ✅ تعديل AppSidebar لاستخدام database permissions
- ✅ إضافة توضيحات في صفحة Role Permissions

---

---

## 🎯 النظام المقترح - Proposed System

### المبدأ الأساسي:
```
الصلاحية النهائية = الصلاحيات الافتراضية (من الدور) + الصلاحيات الاستثنائية (خاصة بالمستخدم)
Final Permission = Default Permissions (from role) + Exception Permissions (user-specific)
```

### الأولوية - Priority:
```
1. صلاحية استثنائية "منع" لمستشفى معين → أعلى أولوية
2. صلاحية استثنائية "منع" عامة
3. صلاحية استثنائية "منح" لمستشفى معين
4. صلاحية استثنائية "منح" عامة
5. صلاحية افتراضية من الدور → أقل أولوية
```

---

## 📊 جدول الأدوار والصلاحيات - Roles & Permissions Matrix

### الأدوار الحالية - Current Roles:

| الكود - Code | الاسم العربي | الاسم English | الوصف - Description |
|-------------|--------------|---------------|-------------------|
| `global_admin` | مدير النظام | Global Admin | صلاحيات كاملة على جميع المستشفيات |
| `hospital_admin` | مدير المستشفى | Hospital Admin | صلاحيات كاملة على مستشفى واحد |
| `facility_manager` | مدير المنشأة | Facility Manager | إدارة المباني والأصول |
| `maintenance_manager` | مدير الصيانة | Maintenance Manager | إدارة الصيانة والفرق |
| `engineer` | مهندس | Engineer | تنفيذ أوامر العمل الفنية |
| `supervisor` | مشرف | Supervisor | الإشراف على الفرق والموافقات |
| `technician` | فني | Technician | تنفيذ أوامر العمل |
| `reporter` | مبلغ | Reporter | الإبلاغ عن أوامر العمل فقط |

---

### التبويبات والصلاحيات - Modules & Permissions:

#### 1️⃣ **Facilities - المنشآت**

**الصلاحيات المتاحة:**
- `facilities.view` - عرض المباني والطوابق والغرف
- `facilities.manage` - إضافة/تعديل/حذف مباني وطوابق وغرف

**الصلاحيات الافتراضية:**
```
✅ global_admin:        view ✓  manage ✓
✅ hospital_admin:      view ✓  manage ✓
✅ facility_manager:    view ✓  manage ✓
✅ maintenance_manager: view ✓  manage ✗
✅ engineer:            view ✓  manage ✗
✅ supervisor:          view ✓  manage ✗
✅ technician:          view ✓  manage ✗
✅ reporter:            view ✓  manage ✗
```

---

#### 2️⃣ **Assets - الأصول**

**الصلاحيات المتاحة:**
- `assets.view` - عرض قائمة الأصول
- `assets.view_costs` - عرض التكاليف والأسعار
- `assets.manage` - إضافة/تعديل أصول
- `assets.delete` - حذف أصول
- `assets.export` - تصدير بيانات الأصول

**الصلاحيات الافتراضية:**
```
✅ global_admin:        view ✓  costs ✓  manage ✓  delete ✓  export ✓
✅ hospital_admin:      view ✓  costs ✓  manage ✓  delete ✓  export ✓
✅ facility_manager:    view ✓  costs ✓  manage ✓  delete ✓  export ✓
✅ maintenance_manager: view ✓  costs ✓  manage ✓  delete ✗  export ✓
✅ engineer:            view ✓  costs ✗  manage ✗  delete ✗  export ✗
✅ supervisor:          view ✓  costs ✗  manage ✗  delete ✗  export ✗
✅ technician:          view ✓  costs ✗  manage ✗  delete ✗  export ✗
✅ reporter:            view ✓  costs ✗  manage ✗  delete ✗  export ✗
```

---

#### 3️⃣ **Work Orders - أوامر العمل**

⚠️ **هذا التبويب مثالي - لا تلمسه - DO NOT TOUCH**

الصلاحيات هنا معقدة ومثالية بالفعل وتعتمد على:
- الدور
- الفريق المعين
- حالة الأمر
- الموقع الجغرافي (للمشرفين)

---

#### 4️⃣ **Inventory - المخزون**

**الصلاحيات المتاحة:**
- `inventory.view` - عرض قائمة المخزون
- `inventory.manage` - إضافة/تعديل/حذف أصناف
- `inventory.transactions` - إدخال/إخراج كميات
- `inventory.export` - تصدير بيانات المخزون

**الصلاحيات الافتراضية:**
```
✅ global_admin:        view ✓  manage ✓  transactions ✓  export ✓
✅ hospital_admin:      view ✓  manage ✓  transactions ✓  export ✓
✅ facility_manager:    view ✓  manage ✓  transactions ✓  export ✓
✅ maintenance_manager: view ✓  manage ✓  transactions ✓  export ✓
✅ engineer:            view ✓  manage ✗  transactions ✓  export ✗
✅ supervisor:          view ✓  manage ✗  transactions ✓  export ✗
✅ technician:          view ✓  manage ✗  transactions ✓  export ✗
✅ reporter:            view ✗  manage ✗  transactions ✗  export ✗
```

---

#### 5️⃣ **Maintenance - خطط الصيانة**

**الصلاحيات المتاحة:**
- `maintenance.view` - عرض خطط الصيانة
- `maintenance.manage` - إدارة خطط ومهام الصيانة
- `maintenance.export` - تصدير بيانات الصيانة

**الصلاحيات الافتراضية:**
```
✅ global_admin:        view ✓  manage ✓  export ✓
✅ hospital_admin:      view ✓  manage ✓  export ✓
✅ facility_manager:    view ✓  manage ✓  export ✓
✅ maintenance_manager: view ✓  manage ✓  export ✓
✅ engineer:            view ✓  manage ✗  export ✗
✅ supervisor:          view ✓  manage ✗  export ✗
✅ technician:          view ✓  manage ✗  export ✗
✅ reporter:            view ✗  manage ✗  export ✗
```

---

#### 6️⃣ **Teams - الفرق**

**الصلاحيات المتاحة:**
- `teams.view` - عرض الفرق
- `teams.manage` - إضافة/تعديل فرق وأعضاء

**الصلاحيات الافتراضية:**
```
✅ global_admin:        view ✓  manage ✓
✅ hospital_admin:      view ✓  manage ✓
✅ facility_manager:    view ✓  manage ✓
✅ maintenance_manager: view ✓  manage ✓
✅ engineer:            view ✓  manage ✗
✅ supervisor:          view ✓  manage ✗
✅ technician:          view ✓  manage ✗
✅ reporter:            view ✗  manage ✗
```

---

#### 7️⃣ **Operations Log - سجل العمليات**

**الصلاحيات المتاحة:**
- `operations_log.view` - عرض سجل العمليات
- `operations_log.export` - تصدير سجل العمليات

**الصلاحيات الافتراضية:**
```
✅ global_admin:        view ✓  export ✓
✅ hospital_admin:      view ✓  export ✓
✅ facility_manager:    view ✓  export ✓
✅ maintenance_manager: view ✓  export ✓
✅ engineer:            view ✗  export ✗
✅ supervisor:          view ✗  export ✗
✅ technician:          view ✗  export ✗
✅ reporter:            view ✗  export ✗
```

---

#### 8️⃣ **Users - المستخدمين**

**الصلاحيات المتاحة:**
- `users.view` - عرض قائمة المستخدمين
- `users.manage` - إضافة/تعديل مستخدمين
- `users.delete` - حذف/إلغاء تفعيل مستخدمين
- `users.assign_roles` - تعيين أدوار
- `users.manage_permissions` - إدارة الصلاحيات الاستثنائية

**الصلاحيات الافتراضية:**
```
✅ global_admin:        view ✓  manage ✓  delete ✓  assign ✓  perms ✓
✅ hospital_admin:      view ✓  manage ✓  delete ✓  assign ✓  perms ✓
✅ facility_manager:    view ✓  manage ✓  delete ✗  assign ✗  perms ✗
✅ maintenance_manager: view ✓  manage ✗  delete ✗  assign ✗  perms ✗
✅ engineer:            view ✗  manage ✗  delete ✗  assign ✗  perms ✗
✅ supervisor:          view ✗  manage ✗  delete ✗  assign ✗  perms ✗
✅ technician:          view ✗  manage ✗  delete ✗  assign ✗  perms ✗
✅ reporter:            view ✗  manage ✗  delete ✗  assign ✗  perms ✗
```

---

#### 9️⃣ **Settings - الإعدادات**

**الصلاحيات المتاحة:**
- `settings.access` - الوصول لصفحة الإعدادات (حصرياً للمدير العام)

**الصلاحيات الافتراضية:**
```
✅ global_admin:        access ✓
❌ hospital_admin:      access ✗
❌ facility_manager:    access ✗
❌ maintenance_manager: access ✗
❌ engineer:            access ✗
❌ supervisor:          access ✗
❌ technician:          access ✗
❌ reporter:            access ✗
```

---

## 🔧 كيف يعمل النظام - How It Works

### 1️⃣ **الصلاحيات الافتراضية - Default Permissions**

يتم تعريفها في صفحة **صلاحيات الأدوار** (`/admin/role-permissions`):

```
مدير المستشفى → تحديد صلاحية "assets.manage" ✓
↓
تُحفظ في جدول role_permissions
↓
كل مستخدم دوره "hospital_admin" يحصل على الصلاحية تلقائياً
```

### 2️⃣ **الصلاحيات الاستثنائية - Exception Permissions**

يتم تعريفها في صفحة **تفاصيل المستخدم** (`/admin/users/[id]`):

#### مثال 1: منح صلاحية إضافية
```
فني → لا يملك "inventory.manage" افتراضياً
↓
مدير المستشفى يضيف له صلاحية استثنائية: "inventory.manage" = منح
↓
الآن هذا الفني المحدد يستطيع إدارة المخزون
```

#### مثال 2: منع صلاحية موجودة
```
مدير منشأة → يملك "assets.delete" افتراضياً
↓
مدير المستشفى يضيف له صلاحية استثنائية: "assets.delete" = منع
↓
الآن هذا المدير المحدد لا يستطيع حذف الأصول
```

#### مثال 3: صلاحية لمستشفى معين (للمدير العام فقط)
```
مدير نظام → له صلاحيات على جميع المستشفيات
↓
يُمنح صلاحية "users.delete" = منع في المستشفى "أ" فقط
↓
يستطيع حذف المستخدمين في جميع المستشفيات إلا المستشفى "أ"
```

---

## 📝 مثال عملي كامل - Complete Example

**السيناريو:** عندنا فني اسمه "أحمد"

### دوره الافتراضي: `technician`

**صلاحياته الافتراضية من الدور:**
```
✅ facilities.view
✅ assets.view
✅ work_orders.* (الصلاحيات المعقدة)
✅ inventory.view
✅ inventory.transactions
✅ maintenance.view
✅ teams.view
```

**لا يملك:**
```
❌ assets.manage
❌ inventory.manage
❌ users.view
```

### الصلاحيات الاستثنائية المضافة له:

1. **منح صلاحية إضافية:**
   ```
   inventory.manage = منح (عام)
   ```
   → الآن يستطيع إدارة المخزون

2. **منع صلاحية موجودة:**
   ```
   inventory.transactions = منع (لمستشفى الرياض فقط)
   ```
   → لا يستطيع إدخال/إخراج كميات في مستشفى الرياض فقط

### النتيجة النهائية:

في **مستشفى جدة:**
```
✅ inventory.view ← من الدور
✅ inventory.manage ← من الاستثناء
✅ inventory.transactions ← من الدور
```

في **مستشفى الرياض:**
```
✅ inventory.view ← من الدور
✅ inventory.manage ← من الاستثناء
❌ inventory.transactions ← ممنوع بالاستثناء
```

---

## 🚀 خطة التنفيذ - Implementation Plan

### المرحلة 1: إدخال البيانات الأولية
1. ✅ ملء جدول `permissions` بجميع الصلاحيات (33 صلاحية)
2. ✅ ملء جدول `role_permissions` بالصلاحيات الافتراضية لكل دور
3. ✅ اختبار صفحة `/admin/role-permissions` للتأكد

### المرحلة 2: تعديل الكود
1. ⬜ تعديل `useCurrentUser` ليستخدم `usePermissions` من قاعدة البيانات
2. ⬜ تعديل جميع الصفحات لاستخدام `permissions.hasPermission()`
3. ⬜ حذف ملف `rolePermissions.ts`
4. ⬜ حذف ملف `permissions.ts` القديم

### المرحلة 3: الاختبار
1. ⬜ اختبار كل دور في كل صفحة
2. ⬜ اختبار الصلاحيات الاستثنائية
3. ⬜ اختبار الصلاحيات الخاصة بمستشفى

---

## ⚠️ ملاحظات مهمة - Important Notes

1. **أوامر العمل:**
   - صلاحيات أوامر العمل **لا تُلمس** - مثالية كما هي
   - معقدة وتعتمد على الحالة والفريق والموقع
   - يتم الاحتفاظ بها في `workOrderStateMachine.ts`

2. **إعدادات النظام:**
   - حصرياً لـ `global_admin` فقط
   - لا صلاحيات استثنائية لها

3. **الأمان:**
   - جميع الفحوصات تتم في الـ Backend (RLS)
   - الصفحات تُخفي العناصر فقط (UX)
   - لا يمكن تجاوز الصلاحيات من المتصفح

4. **الأداء:**
   - `usePermissions` يستخدم cache ذكي
   - يجلب البيانات مرة واحدة عند تسجيل الدخول
   - لا يؤثر على سرعة التطبيق

---

## ✅ الخلاصة - Summary

| العنصر | الحالي | المقترح |
|--------|--------|---------|
| مصدر الصلاحيات | ملف كود ثابت | قاعدة بيانات |
| الصلاحيات الافتراضية | `rolePermissions.ts` | جدول `role_permissions` |
| الصلاحيات الاستثنائية | ❌ غير فعالة | ✅ جدول `user_permissions` |
| التخصيص لكل مستشفى | ❌ غير متاح | ✅ متاح |
| الإدارة | يحتاج مبرمج | يدار من لوحة التحكم |

---

## 📞 جاهز للتنفيذ؟

الآن كل شيء واضح ومشروح. قل "نفذ" لبدء التنفيذ!

